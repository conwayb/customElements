{
  "name": "templates",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore --fix src"
  },
  "devDependencies": {
    "vite": "^4.1.0",
    "vite-plugin-handlebars": "^1.6.0"
  }
}
